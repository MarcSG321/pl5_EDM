---
title: "DEPLOYMENT"
output: html_document
date: '2023-05-02'
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```


```{r}
library(randomForest)
datos=read.csv('df_modificado.csv')
datos=datos[,-1]
modelo=randomForest(cnt~.,data=datos)

```

```{r}
library(cowplot)
library(gridExtra)
library(ggplot2)

par(mfrow = c(2, 2))
# Genera los PDPs de cada variable
pdp1 <- partialPlot(modelo, datos, temp,plot=TRUE)
pdp2 <- partialPlot(modelo, datos, days_since_2011,plot=TRUE)
pdp3 <- partialPlot(modelo, datos, hum,plot=TRUE)
pdp4 <- partialPlot(modelo, datos, windspeed,plot=TRUE)





```

